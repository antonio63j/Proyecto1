import { DirectivamenuDirective } from './directivamenu.directive';

describe('DirectivamenuDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivamenuDirective();
    expect(directive).toBeTruthy();
  });
});
